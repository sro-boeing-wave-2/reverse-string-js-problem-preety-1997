// The intent of this file is to reverse
// the string using built in functions.
const reverse = function reverseString(str) {
  return (str.split('').reverse().join(''));
};
module.exports = reverse;
